{
  "name": "vacate",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rimraf ./dist/ && rimraf ./.parcel-cache/",
    "dev": "npm run clean && parcel src/index.hbs",
    "build": "npm run clean && parcel build src/index.hbs",
    "start": "npm run clean && parcel build src/index.hbs && node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@anikin/parcel-transformer-handlebars": "^1.0.9",
    "@parcel/transformer-sass": "^2.7.0",
    "express": "^4.18.1",
    "parcel": "^2.7.0",
    "rimraf": "^3.0.2"
  },
  "dependenciesComments": {
    "rimraf": "We're using rimraf so windows users can delete folders too. Just for now, we need to delete dist and parcel cache to avoid bugs"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
